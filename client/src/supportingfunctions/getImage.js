export function getImage(buffer) {
   console.log(buffer);  
   /*const blob = new Blob(buffer.loadsrc[0].data.data,{type:buffer.loadsrc[0].format});
   console.log(blob);
   const url = URL.createObjectURL(blob);
   URL.revokeObjectURL(blob);
   console.log(url);
   return url;*/
}